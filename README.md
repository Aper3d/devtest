# This project demonstrates a simple DevOps setup using Docker Compose

- Node.js backend servise
- PostgresSQL database with presistent volume
- Environment variables via .env
- Healthcheck endpoint for service and datababse
